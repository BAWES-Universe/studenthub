

server {
    listen 80;
    server_name employer.api.studenthub.co;

    location / {
        proxy_pass http://127.0.0.1:8080/company/web; # Replace 8080 with your Docker container's exposed port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Handle pretty URLs for Yii framework
        try_files $uri $uri/ /index.php;
    }

    access_log /var/log/nginx/employer_access.log;
    error_log /var/log/nginx/employer_error.log;
}

server {
    listen 443 ssl;
    server_name employer.api.studenthub.co;

    ssl_certificate /etc/ssl/certs/ssl-cert-snakeoil.pem;
    ssl_certificate_key /etc/ssl/private/ssl-cert-snakeoil.key;

    location / {
        proxy_pass http://127.0.0.1:8080/company/web; # Replace 8080 with your Docker container's exposed port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Handle pretty URLs for Yii framework
        try_files $uri $uri/ /index.php;
    }

    access_log /var/log/nginx/employer_access.log;
    error_log /var/log/nginx/employer_error.log;
}

server {
    listen 80;
    server_name admin.api.studenthub.co;

    location / {
        proxy_pass http://127.0.0.1:8080/admin/web; # Replace 8080 with your Docker container's exposed port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Handle pretty URLs for Yii framework
        try_files $uri $uri/ /index.php;
    }

    access_log /var/log/nginx/admin_access.log;
    error_log /var/log/nginx/admin_error.log;
}

server {
    listen 443 ssl;
    server_name admin.api.studenthub.co;

    ssl_certificate /etc/ssl/certs/ssl-cert-snakeoil.pem;
    ssl_certificate_key /etc/ssl/private/ssl-cert-snakeoil.key;

    location / {
        proxy_pass http://127.0.0.1:8080/admin/web; # Replace 8080 with your Docker container's exposed port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Handle pretty URLs for Yii framework
        try_files $uri $uri/ /index.php;
    }

    access_log /var/log/nginx/admin_access.log;
    error_log /var/log/nginx/admin_error.log;
}


server {
    listen 80;
    server_name candidate.api.studenthub.co;

    location / {
        proxy_pass http://127.0.0.1:8080/candidate/web; # Replace 8080 with your Docker container's exposed port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Handle pretty URLs for Yii framework
        try_files $uri $uri/ /index.php;
    }

    access_log /var/log/nginx/candidate_access.log;
    error_log /var/log/nginx/candidate_error.log;
}

server {
    listen 443 ssl;
    server_name candidate.api.studenthub.co;

    ssl_certificate /etc/ssl/certs/ssl-cert-snakeoil.pem;
    ssl_certificate_key /etc/ssl/private/ssl-cert-snakeoil.key;

    location / {
        proxy_pass http://127.0.0.1:8080/candidate/web; # Replace 8080 with your Docker container's exposed port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Handle pretty URLs for Yii framework
        try_files $uri $uri/ /index.php;
    }

    access_log /var/log/nginx/candidate_access.log;
    error_log /var/log/nginx/candidate_error.log;
}

server {
    listen 80;
    server_name v.studenthub.co;

    location / {
        proxy_pass http://127.0.0.1:8080/verification/web; # Replace 8080 with your Docker container's exposed port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Handle pretty URLs for Yii framework
        try_files $uri $uri/ /index.php;
    }

    access_log /var/log/nginx/verification_access.log;
    error_log /var/log/nginx/verification_error.log;
}

server {
    listen 443 ssl;
    server_name v.studenthub.co;

    ssl_certificate /etc/ssl/certs/ssl-cert-snakeoil.pem;
    ssl_certificate_key /etc/ssl/private/ssl-cert-snakeoil.key;

    location / {
        proxy_pass http://127.0.0.1:8080/verification/web; # Replace 8080 with your Docker container's exposed port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Handle pretty URLs for Yii framework
        try_files $uri $uri/ /index.php;
    }

    access_log /var/log/nginx/verification_access.log;
    error_log /var/log/nginx/verification_error.log;
}


server {
    listen 80;
    server_name inspector.api.studenthub.co;

    location / {
        proxy_pass http://127.0.0.1:8080/inspector/web; # Replace 8080 with your Docker container's exposed port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Handle pretty URLs for Yii framework
        try_files $uri $uri/ /index.php;
    }

    access_log /var/log/nginx/inspector_access.log;
    error_log /var/log/nginx/inspector_error.log;
}

server {
    listen 443 ssl;
    server_name inspector.api.studenthub.co;

    ssl_certificate /etc/ssl/certs/ssl-cert-snakeoil.pem;
    ssl_certificate_key /etc/ssl/private/ssl-cert-snakeoil.key;

    location / {
        proxy_pass http://127.0.0.1:8080/inspector/web; # Replace 8080 with your Docker container's exposed port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Handle pretty URLs for Yii framework
        try_files $uri $uri/ /index.php;
    }

    access_log /var/log/nginx/inspector_access.log;
    error_log /var/log/nginx/inspector_error.log;
}

server {
    listen 80;
    server_name staff.api.studenthub.co;

    location / {
        proxy_pass http://127.0.0.1:8080/staff/web; # Replace 8080 with your Docker container's exposed port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Handle pretty URLs for Yii framework
        try_files $uri $uri/ /index.php;
    }

    access_log /var/log/nginx/staff_access.log;
    error_log /var/log/nginx/staff_error.log;
}

server {
    listen 443 ssl;
    server_name staff.api.studenthub.co;

    ssl_certificate /etc/ssl/certs/ssl-cert-snakeoil.pem;
    ssl_certificate_key /etc/ssl/private/ssl-cert-snakeoil.key;

    location / {
        proxy_pass http://127.0.0.1:8080/staff/web; # Replace 8080 with your Docker container's exposed port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Handle pretty URLs for Yii framework
        try_files $uri $uri/ /index.php;
    }

    access_log /var/log/nginx/staff_access.log;
    error_log /var/log/nginx/staff_error.log;
}

